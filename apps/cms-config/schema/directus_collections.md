Collections:
- countries (slug, name, region, enabled, video_group)
- videos (group, locale, url, title)
- pages (slug, locale, title, metaTitle, metaDesc, bodyMd, videoUrl)

Rule: video auto-detect by locale, fallback EN.